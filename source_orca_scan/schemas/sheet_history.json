{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "sheet_id": { "type": "string", "description": "Id of the sheet this change belongs to (added by the connector)." },
    "_id": { "type": "string", "description": "History entry id (not the row id)." },
    "_change": { "type": ["string", "null"], "description": "add, update or delete." },
    "_changedOn": { "type": "string", "format": "date-time", "description": "When the change happened (ISO 8601, UTC). The incremental cursor." },
    "_changedBy": { "type": ["string", "null"], "description": "Email of the user who made the change." },
    "_changedUsing": { "type": ["string", "null"], "description": "Device or client the change was made from." }
  }
}
